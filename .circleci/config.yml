version: 2.1
orbs:
  node: circleci/node@5.0.2

jobs:
  build:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - node/install-yarn
workflows:
  build:
    jobs:
      - build
